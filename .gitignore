.env
*.db
*.db-wal
*.db-shm
__pycache__/
*.pyc
logs/
data/cache/
backups/
venv/
.env.save
*.env.save
.DS_Store
.claude/
.key_rotation_date
data/daemon_heartbeat
data/openclaw_progress.json
# Ignore the generated vault, but NOT the human feedback zone: git can only
# re-include a subdir if the parent dir itself isn't excluded, so match
# vault/* (children) rather than vault/ (the dir), then un-ignore feedback/.
vault/*
!vault/feedback/
