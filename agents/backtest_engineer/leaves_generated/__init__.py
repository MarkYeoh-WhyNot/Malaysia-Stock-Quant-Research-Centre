"""AI-synthesized DSL leaves (agents/leaf_synthesizer/LeafSynthesizer).

Every module here was written by an LLM pipeline (Opus plans, Haiku codes,
Sonnet reviews + runs the generated test) in response to a genuinely-
unrepresentable formula, approved only after its own test passed and a
safety review found no lookahead/side-effects/duplicate semantics. Kept
physically separate from the hand-authored catalog in ../signal_dsl.py for
auditability — see leaf_synthesis_attempts for the full record of every
attempt, approved or not.
"""
